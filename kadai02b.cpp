#include <iostream>
#include <cmath>
void squarify(Rectangle* ptr_to_rect){
    double x,y,area;
    area = (ptr_to_rect.x_max - ptr_to_rect.x_min) * (ptr_to_rect.y_max - ptr_to_rect.y_min);
    x = (ptr_to_rect.x_max + ptr_to_rect.y_max) / 2;
    y = (ptr_to_rect.x_max + ptr_to_rect.x_min) / 2;
}
int main(){

}